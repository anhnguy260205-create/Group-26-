# Optional — LLM providers for burnout reasoning + closing reflections.
# Tried in order: Foundry, then Anthropic, then built-in templates. Leave all
# unset and the app still works fully on templated text.

# Microsoft Foundry deployment's "Consume" tab -> full chat-completions URL
FOUNDRY_ENDPOINT=
FOUNDRY_API_KEY=
# Only needed for unified multi-model endpoints
FOUNDRY_MODEL=

ANTHROPIC_API_KEY=
